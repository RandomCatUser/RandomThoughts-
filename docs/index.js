/* ============================================================
   Random Thoughts — Homepage script (index.html)
   Page-specific behavior only; theme + player live in common.js.
   ============================================================ */
(function () {
    "use strict";
    document.addEventListener('DOMContentLoaded', function () {
        /* Any homepage-specific behaviors can go here. */
    });
})();
